server:
  runmode: debug #or debug/test/release
  port: 8888

ext:
  versionName: "0.0.1"
  versionCode: 1

  proxy:
    google:
      remote: "www.google.com"
      local: "g.localhost:8888"
    gmail:
      remote: "mail.google.com"
      local: "m.localhost:8888"
    googleAccount:
      remote: "accounts.google.com"
      # if remoteSchema or localSchema is http, you can ignore it
      remoteSchema: "http"
      local: "a.localhost:8888"
    baidu:
      remote: "www.baidu.com"
      remoteSchema: "https"
      local: "baidu.localhost:8888"